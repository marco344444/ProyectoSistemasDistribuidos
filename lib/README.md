# Librerias Java externas

Para habilitar persistencia JDBC con PostgreSQL, descarga el driver JDBC oficial y colocalo en esta carpeta.

Archivo esperado (ejemplo):

- postgresql-42.7.4.jar

Puedes usar versiones mas nuevas, manteniendo extension `.jar`.

Luego ejecuta backend con classpath incluyendo `lib/*.jar`.
